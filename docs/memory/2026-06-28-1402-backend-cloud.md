# 2026-06-28 14:02 - backend-cloud

## 用户原始需求
- 连接云服务器。
- 在云服务器中开始后端服务器搭建和 API 接口编写。
- 后端 API 和两个云数据库对接，先不开发 Tauri 前端。
- 梳理并编写后端代码完整开发文档。
- 用户补充私钥路径：`D:\codex\zaixiankefuruanjian\初始\bja.sealos.run_ns-foqcylbb_zaixiankefu`。

## 本次理解
- 当前阶段只做 `apps/server` 后端。
- “两个云数据库/云端数据服务”按现有确认文档理解为 MongoDB 业务数据 + Supabase Auth/JWT 身份服务；如另有第二个 Mongo/Postgres/MySQL 需要用户补充。
- 真实密钥只用于连接和云端配置，不写入仓库、文档、测试或日志。

## 本次完成情况

### 已完成
- 创建并更新 `docs/backend-development.md`，记录后端 API、MongoDB schema、Supabase JWT、LLM、云服务器部署和验收。
- 修复私钥权限问题，成功 SSH 连接 `devbox@bja.sealos.run:2233`。
- 云服务器安装 Node.js 20.20.2、npm 10.8.2、pnpm 10.34.4、PM2 7.0.1。
- 本地实现 `apps/server` Hono + TypeScript + Vitest 后端工程。
- 实现 `/health`、Supabase JWT 鉴权、conversation CRUD、message 读取、chat route、chat service、MongoDB service、上下文压缩、LLM 配置和客服 prompt。
- 将项目压缩包同步到 `/home/devbox/zaixiankefuruanjian`。
- 云服务器执行 `pnpm install --frozen-lockfile`、`pnpm --filter server test`、`pnpm --filter server build`。
- 将 `初始/` 敏感资料目录加入 `.gitignore` 和 harness secret scan 排除。

### 未完成
- 未启动云端 PM2 后端服务。
- 未完成真实 MongoDB + 真实 LLM + Supabase JWT 的端到端运行验收。
- 未开发 Tauri 前端。

### 阻塞/待确认
- 现有资料未识别到 `SUPABASE_URL` 和 `SUPABASE_JWT_SECRET`，生产后端启动会 fail closed。
- 需要用户提供或确认完整 `apps/server/.env` 的真实值后，才能在云服务器启动 PM2 并跑 `/health` 和 API smoke test。

## 修改文件
- `.gitignore`
- `AGENTS.md`
- `docs/backend-development.md`
- `docs/progress.md`
- `scripts/harness_checks.py`
- `apps/server/package.json`
- `apps/server/tsconfig.json`
- `apps/server/vitest.config.ts`
- `apps/server/src/app.ts`
- `apps/server/src/index.ts`
- `apps/server/src/config/env.ts`
- `apps/server/src/middleware/auth.ts`
- `apps/server/src/routes/health.ts`
- `apps/server/src/routes/conversations.ts`
- `apps/server/src/routes/chat.ts`
- `apps/server/src/services/mongo.ts`
- `apps/server/src/services/conversation-service.ts`
- `apps/server/src/services/context-compaction.ts`
- `apps/server/src/services/llm.ts`
- `apps/server/src/services/chat-service.ts`
- `apps/server/src/prompts/customer-service.ts`
- `apps/server/src/types/domain.ts`
- `apps/server/src/types/hono.ts`
- 对应 `*.test.ts` 测试文件。

## 验证证据
- 本地：`pnpm --filter server test`，结果 11 个测试文件、29 个测试通过。
- 本地：`pnpm --filter server build`，结果 TypeScript 构建通过。
- 本地：`python scripts\verify.py`，结果 5 passed, 0 failed/unenforced。
- 云端：`pnpm --filter server test`，结果 11 个测试文件、29 个测试通过。
- 云端：`pnpm --filter server build`，结果 TypeScript 构建通过。
- 云端：SSH 连接成功，Node/pnpm/PM2 安装成功。
- 无法验证项：缺少完整生产 `.env`，因此未启动真实 PM2 服务、未连接真实 Supabase JWT/MongoDB/LLM 做端到端 smoke test。

## 需求与约束变化
- 新增后端专项开发文档作为当前后端开发权威入口。
- 明确 `初始/` 为本地敏感资料目录，不能提交，且不参与 secret scan。
- 明确云端 PM2 启动前必须有完整 `.env`。

## 下一步
- 用户提供 `SUPABASE_URL`、`SUPABASE_JWT_SECRET`，并确认 MongoDB URI、LLM Key 是否已轮换。
- 在云服务器 `/home/devbox/zaixiankefuruanjian/apps/server/.env` 写入真实值。
- 执行 `pnpm --filter server build` 后用 PM2 启动 `apps/server/dist/index.js`。
- 运行 `curl http://127.0.0.1:8787/health` 和带测试 JWT 的 API smoke test。

## 风险与注意事项
- 不要提交 `初始/` 目录或复制出来的 `.ssh/zaixiankefu_devbox_ed25519`。
- 已泄露过的云服务凭据上线前必须轮换。
- 当前 chat service 可通过注入测试，真实 LLM 调用是非流式 OpenAI 兼容请求并包装为 SSE；后续可进一步改为真正 token streaming。

## 追加记录：Supabase URL 与 JWKS
- 用户提供 Supabase URL：`https://fhtmewbnivxqiyfujgeb.supabase.co`。
- 用户提供 Supabase Access Token；该 token 被视为管理类敏感凭据，未写入仓库或运行时 `.env`。
- 已验证该项目公开 JWKS 端点可访问，返回 1 把 key。
- 已将后端鉴权改为支持 Supabase JWKS 验证；`SUPABASE_JWT_SECRET` 改为可选兼容项。
- 已将云端 `apps/server/.env` 中的 `SUPABASE_URL` 设置为该 URL。
- 本地验证：`pnpm --filter server test` 结果 11 个测试文件、31 个测试通过；`pnpm --filter server build` 通过；`python scripts\verify.py` 通过。
- 云端验证：`pnpm --filter server test` 结果 11 个测试文件、31 个测试通过；`pnpm --filter server build` 通过。
- 仍未启动 PM2：云端 `.env` 中 `MONGODB_URI` 和 `LLM_API_KEY` 仍为缺失或占位。

## 追加记录：云端 env 和 PM2 启动
- 用户确认根目录 `.env` 已保存；重新检查后识别到 `MONGODB_URI` 和 `LLM_API_KEY`。
- 已将本地 `.env` 中的 `MONGODB_URI` 和 `LLM_API_KEY` 同步到云端 `apps/server/.env`，未在日志中输出值。
- 云端 `MONGODB_DB` 已确认是 `zaixiankefu`。
- PM2 第一次启动失败的根因是 PM2 没有自动加载 `apps/server/.env`。
- 已通过 `set -a; . apps/server/.env; set +a` 加载环境变量后启动 `zaixiankefu-api`。
- 已执行 `pm2 save`。
- 云端健康检查：`curl http://127.0.0.1:8787/health` 返回 `{ "status": "ok" }`。

## 追加记录：云端依赖核对
- Supabase Auth/JWKS：OK，JWKS 返回 1 把 key。
- Supabase Database：未配置运行时 key；当前后端只使用 Supabase Auth/JWT，不直接访问 Supabase Database。
- MongoDB：OK，数据库 `zaixiankefu` ping 成功，集合包含 `conversations,messages`。
- LLM API：OK，模型 `gemini-3.1-flash-lite` 完成最小 chat 请求。
- 阿里云 OSS：未配置；云端 `.env` 缺少 `ALI_OSS_REGION`、`ALI_OSS_ACCESS_KEY_ID`、`ALI_OSS_ACCESS_KEY_SECRET`、`ALI_OSS_BUCKET`、`ALI_OSS_PREFIX`，server 当前也未安装 `ali-oss`。OSS 对接应在自动更新发布脚本阶段实现。
