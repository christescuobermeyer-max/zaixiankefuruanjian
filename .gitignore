# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
coverage/
*.tsbuildinfo

# Tauri / Rust output
apps/desktop/src-tauri/target/
target/

# Environment and secrets
.env
.env.*
!.env.example
*.pem
*.key
*private*
*secret*
*.p8
*.p12
*.sig

# Local sensitive source document retained outside version control
初始需求
初始/

# SSH private keys for cloud server (never commit)
bja.sealos.run_*
*_ed25519
id_*

# Tauri updater signing keys (never commit the private key)
.tauri-keys/

# Runtime output (logs, playwright artifacts)
output/

# Logs and local state
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
.idea/
.vscode/

# 本地参考素材 / 设计稿，不纳入版本控制
/AI客服prompt/
/AI客服按钮原型 (离线版).html
/Image 1.png
/前端ui原型图/
